# Automation test environment variables
# Copy this file to .env and fill in the values

# Must match APP_SECRET in the root .env — used by API tests for X-App-Key header
APP_SECRET=shopvn-dev-app-secret
